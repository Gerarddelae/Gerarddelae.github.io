---
const experiences = [
  {
    year: '2024 - Present',
    title: 'Analista y Desarrollador de Software',
    company: 'En formación y proyectos freelance',
    description: 'Enfoque en Machine Learning, Angular y Spring Boot. Desarrollando soluciones robustas para hotelería y herramientas de IA SQL.',
    icon: '💻'
  },
  {
    year: '2022 - 2024',
    title: 'Ingeniería Civil & Análisis de Datos',
    company: 'Proyectos Estructurales',
    description: 'Gestión de proyectos y diseño estructural aplicando herramientas digitales para la optimización de procesos.',
    icon: '🏗️'
  },
  {
    year: '2021',
    title: 'Inicio en Programación',
    company: 'Autodidacta',
    description: 'Primeros pasos en algoritmos, estructuras de datos y desarrollo web para automatizar tareas de ingeniería.',
    icon: '🚀'
  }
];
---

<div class="timeline">
  {experiences.map((exp, index) => (
    <div class="timeline-item" style={`--delay: ${index * 0.2}s`}>
      <div class="timeline-dot"></div>
      <div class="timeline-content">
        <div class="header">
          <span class="year">{exp.year}</span>
          <span class="icon">{exp.icon}</span>
        </div>
        <h3 class="title">{exp.title}</h3>
        <p class="company">{exp.company}</p>
        <p class="description">{exp.description}</p>
      </div>
    </div>
  ))}
</div>

<style>
  .timeline {
    position: relative;
    max-width: 800px;
    margin: 3rem auto;
    padding: 0 1rem;
  }

  /* Vertical line */
  .timeline::before {
    content: '';
    position: absolute;
    left: 2rem;
    top: 1rem;
    bottom: 0;
    width: 2px;
    background: var(--color-border);
    opacity: 0.5;
  }

  .timeline-item {
    position: relative;
    padding-left: 4.5rem;
    margin-bottom: 3rem;
    opacity: 0;
    transform: translateY(20px);
    animation: reveal 0.8s forwards;
    animation-delay: var(--delay);
  }

  @keyframes reveal {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .timeline-dot {
    position: absolute;
    left: 1.5rem;
    top: 0.5rem;
    width: 1rem;
    height: 1rem;
    background: var(--color-accent);
    border-radius: 50%;
    border: 4px solid var(--color-bg);
    box-shadow: 0 0 0 2px var(--color-accent);
    z-index: 2;
  }

  .timeline-content {
    background: rgba(var(--color-bg), 0.5);
    backdrop-filter: blur(10px);
    border: 1px solid var(--color-border);
    border-radius: 1.5rem;
    padding: 1.5rem;
    transition: all 0.3s;
  }

  .timeline-content:hover {
    border-color: var(--color-accent);
    transform: translateX(10px);
    background: rgba(var(--color-bg-secondary), 0.5);
    box-shadow: 0 10px 30px -10px rgba(0, 0, 0, 0.2);
  }

  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
  }

  .year {
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--color-accent);
    background: color-mix(in srgb, var(--color-accent) 15%, transparent);
    padding: 0.25rem 0.75rem;
    border-radius: 99px;
  }

  .icon { font-size: 1.25rem; }

  .title {
    font-size: 1.25rem;
    font-weight: 800;
    margin: 0.5rem 0 0.25rem 0;
    color: var(--color-text);
  }

  .company {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--color-text-secondary);
    margin-bottom: 0.75rem;
  }

  .description {
    font-size: 1rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    margin: 0;
  }
</style>
